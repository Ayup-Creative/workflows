# .github/workflows/release.yml

name: Release

"on":
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    name: Prepare or create release
    uses: Ayup-Creative/workflows/.github/workflows/release.yml@v1
    secrets: inherit
